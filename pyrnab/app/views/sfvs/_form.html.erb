<%= form_for(@sfv) do |f| %>
  <% if @sfv.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@sfv.errors.count, "error") %> prohibited this sfv from being saved:</h2>

      <ul>
      <% @sfv.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :id %><br>
    <%= f.number_field :id %>
  </div>
  <div class="field">
    <%= f.label :data %><br>
    <%= f.text_area :data %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
